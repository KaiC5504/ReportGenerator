<Window x:Class="ReportGenerator.App.Views.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available"
        Width="640"
        Height="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="A new version is available."
                   FontSize="22"
                   FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,12"
                   Text="{Binding SummaryText}"
                   Foreground="#5B6570"
                   TextWrapping="Wrap" />

        <TextBox Grid.Row="2"
                 Text="{Binding ReleaseNotes}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 Background="#FAFBFC"
                 BorderBrush="#D4D8DE" />

        <StackPanel Grid.Row="3"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="120"
                    Margin="0,0,10,0"
                    Content="Later"
                    IsCancel="True"
                    Click="LaterButton_OnClick" />
            <Button Width="140"
                    Content="Install Now"
                    IsDefault="True"
                    Click="InstallButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
